
# Habilita el motor de reescritura de URLs
RewriteEngine On

# Establece la base de la URL para las reglas. Esto es CRUCIAL para subdirectorios.
RewriteBase /gestion-sistema/

# Condición: si el recurso solicitado no es un archivo existente
RewriteCond %{REQUEST_FILENAME} !-f

# Condición: si el recurso solicitado no es un directorio existente
RewriteCond %{REQUEST_FILENAME} !-d

# Regla: para cualquier otra petición, sirve el archivo index.html
# Esto permite que el enrutador de React tome el control.
RewriteRule . index.html [L]
